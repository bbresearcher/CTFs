1. Commands to run to initiate directory
```
mkdir ctf_dir
cd ctf_dir
forge init --no-git
forge install --no-git OpenZeppelin/openzeppelin-contracts
```
2. Copy to files to the correct directories and create files from CTF
3. Test with ```forge test -vv```
